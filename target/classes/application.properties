server.port=8081

# Postgres datasource
spring.datasource.url=jdbc:postgresql://localhost:5432/career_platform?currentSchema=public
spring.datasource.username=career_user
spring.datasource.password=chaimanour@22
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=public

# JWT settings
jwt.secret=change_this_to_a_strong_secret
jwt.expiration=900000

# Admin bootstrap user (change before production!)
admin.default.email=admin@capgemini.com
admin.default.password=ChangeMe123!

# File Upload Configuration
cv.upload.dir=uploads/cvs/
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
# AI Microservices (via API Gateway on port 9000)
# ========================
# CV Skill Extraction service (gateway -> 8004)
ai.service.url=http://localhost:9000/api/extract
# Matching microservice (gateway -> 8001). Use explicit final endpoint so the URL builder leaves it untouched.
matching.microservice.url=http://localhost:9000/api/match/match/inline
# Career Recommendation service (gateway -> 8002)
career.service.url=http://localhost:9000/api/recommend

# Frontend URL used to build password reset links
app.frontend.base-url=http://localhost:5173

# Email / SMTP settings (configure for your environment)
app.email.from=chaimahichri524@gmail.com
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=chaimahichri524@gmail.com
spring.mail.password=pgnslcekwjywmzih
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# Ensure schema scripts run on startup to keep constraints up-to-date
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true